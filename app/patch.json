{
    "patch_policy": [""],
    "patch_nodes": [{
        "container": {
            "snapshot": "ss-8qgu9oel",
            "zone": "pek3"
        },
        "patch": [{
            "mount_role": "replica",
            "mount_point": "/upgrade",
            "mount_options": "defaults,noatime",
            "filesystem": "ext4",
            "cmd": "/upgrade/opt/mongodb/3.4.17/bin/upgrade.sh"
        }],
        "rollback": [{
            "mount_role": "replica",
            "mount_point": "/upgrade",
            "mount_options": "defaults,noatime",
            "filesystem": "ext4",
            "cmd": "/upgrade/opt/mongodb/3.4.17/bin/upgrade.sh rollback"
        }]
    }]
}
